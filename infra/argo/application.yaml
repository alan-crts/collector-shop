apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: collector-shop-prod
  namespace: argocd
spec:
  project: default
  source:
    # TODO: Remplacer par l'URL de votre dépôt Github
    repoURL: 'https://github.com/VOTRE_COMPTE_GITHUB/collector-shop.git'
    path: infra/k8s
    targetRevision: main
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: collector-shop-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
